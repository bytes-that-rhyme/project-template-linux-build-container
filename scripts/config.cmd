:: Set configuration variables here.
:: %BASE% is the root directory of the project,
:: which is the parent directory of the 'scripts'
:: directory that contains this config.
set DEV_DIRECTORY=%BASE%\src
set MACHINE_NAME=podman-template
